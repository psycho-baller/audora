[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "audora-transcript-lab"
version = "0.1.0"
description = "Local transcript weakness lab for communication analysis."
requires-python = ">=3.10"

[tool.setuptools]
packages = ["transcript_lab"]
